# 📦 Requerimientos agrupados
# ---------------------------
# Este archivo actúa como wrapper para los nuevos archivos modulares en
# `requirements/`. Usa los comandos de abajo según el caso:
#
# - `pip install -r requirements/base.txt` → pipeline/inferencia
# - `pip install -r requirements/training.txt` → entrenamiento completo
# - `pip install -r requirements/apps/image_lab.txt` → app Image Lab
# - `pip install -r requirements/apps/weapon_monitor.txt` → app Weapon Monitor
#
# Mantenemos esta referencia por compatibilidad con scripts existentes.

-r requirements/base.txt